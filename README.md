# PICSimLab python rcontrol wrapper library

Python library for controlling the PICSimLab simulator using the rcontrol remote control protocol.

## Install

```
python3 -m pip install PICSimlab_rcontrol
```
